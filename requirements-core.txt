# Core dependencies (always required)
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
requests>=2.31.0
gunicorn>=21.2.0

# Bedrock API integration
pydantic>=2.0.0

# GCS (used by news headlines endpoint when GCS_BUCKET env is set)
google-cloud-storage>=2.10.0
