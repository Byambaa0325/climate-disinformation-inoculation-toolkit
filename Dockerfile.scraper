# Scraper-only image for Cloud Run Job
FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir requests google-cloud-storage

COPY scripts/fetch_news.py ./scripts/fetch_news.py

ENV PYTHONUNBUFFERED=1

CMD ["python", "scripts/fetch_news.py"]
